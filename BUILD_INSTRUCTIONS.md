# ?? APK Build Instructions - ????? ????????

## ????? ???????? (Prerequisites)

### 1. Node.js ?? npm
- Node.js 18 ?? ?? ???? version ???? ????????
- ?????? ?????: [Node.js Download](https://nodejs.org/)

### 2. Java Development Kit (JDK)
- JDK 11 ?? ?? ???? version ???? ????????
- ?????? ?????: [OpenJDK](https://adoptium.net/) ?? Oracle JDK

### 3. Android SDK
- Android SDK ??????? ?? ??? ????????
- ANDROID_HOME environment variable set ?????
- ?????? ?????: [Android Studio](https://developer.android.com/studio)

### 4. React Native CLI
```bash
npm install -g react-native-cli
```

## ?? APK Build ??? ?????

### Method 1: Build Script Use ???? (????)

```bash
# Dependencies install ?????
npm install

# Build script run ?????
./build-apk.sh
```

Build successful ???, `?????_????????_[timestamp].apk` file ?? project root ??? create ??.

### Method 2: Manual Build

```bash
# 1. Dependencies install ?????
npm install

# 2. Android folder ??? ????
cd android

# 3. Debug APK build ?????
./gradlew assembleDebug

# 4. Release APK build ????? (?????? ??? ??)
./gradlew assembleRelease

# 5. Build ???? APK file ?? locate ?????
# Debug: android/app/build/outputs/apk/debug/app-debug.apk
# Release: android/app/build/outputs/apk/release/app-release.apk
```

## ?? APK Download ??????

### Option 1: Local Build
Build script run ???? ????, APK file ?? `?????_????????_[timestamp].apk` ????? project root ??? ??.

### Option 2: Android Device ??? Direct Install ?????

```bash
# USB debugging enabled ????? device ?? connect ?????
adb devices

# APK install ?????
adb install ?????_????????_[timestamp].apk
```

### Option 3: Cloud Build (CI/CD)
- GitHub Actions
- GitLab CI
- Jenkins
- Codemagic
- App Center

## ?? Build Configuration

### Package Name
- Application ID: `com.mudhalkalanakarana`
- Package Name: `com.mudhalkalanakarana`

### Version
- Version Code: 1
- Version Name: 1.0

### Minimum SDK
- Minimum SDK: 21 (Android 5.0 Lollipop)
- Target SDK: 33 (Android 13)

## ?? Build Output Locations

### Debug APK
```
android/app/build/outputs/apk/debug/app-debug.apk
```

### Release APK
```
android/app/build/outputs/apk/release/app-release.apk
```

### Signed Release APK (??????? build ???? ???)
```
?????_????????_[timestamp].apk (project root)
```

## ?? Troubleshooting

### Error: JAVA_HOME not set
```bash
export JAVA_HOME=/path/to/java
export ANDROID_HOME=/path/to/android/sdk
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
```

### Error: Gradle build failed
```bash
cd android
./gradlew clean
./gradlew assembleRelease
```

### Error: Node modules missing
```bash
npm install
npm install --legacy-peer-deps  # If peer dependency issues
```

### Error: Metro bundler issues
```bash
npm start -- --reset-cache
```

## ?? Quick Start Commands

```bash
# 1. Dependencies install
npm install

# 2. Build APK
./build-apk.sh

# 3. Find APK
ls -lh ?????_????????_*.apk

# 4. Install to device (optional)
adb install ?????_????????_*.apk
```

## ?? Notes

- Debug keystore automatically create ?? build script run ???? ???
- Release build ????? signed keystore ???? ?????? (production use ??? ???)
- APK file size approximately 30-50 MB ??? ???
- Build time depends on your machine speed (usually 5-15 minutes)

## ?? Success!

APK build successful ???, ???? device ??? install ????? app ?? use ????? ???????!
